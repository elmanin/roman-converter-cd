{
  "name": "roman-converter",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha-headless-chrome -f test.html -a no-sandbox -a disable-setuid-sandbox",
    "lint": "eslint script.js tests.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "mocha-headless-chrome": "^4.0.0"
  }
}
